# Refer for explanation to following link:
# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: test
      run: pnpm lint {staged_files}
      glob: "*.{js,ts}"

    - name: test
      run: pnpm test
